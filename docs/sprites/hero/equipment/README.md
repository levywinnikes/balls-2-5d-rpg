# Equipment body specs — archived (alpha)

**Alpha:** corpo fixo `hero_default`. Equipamento = stats + ícone (`public/assets/items/{id}.png`).

Não gerar overlays de elmo/armadura nesta pasta até fase 2 (`docs/CHARACTER_VISUAL_SCOPE.md`).

| Arquivo | Status |
| :--- | :--- |
| `leather-helmet.spec.json` | Rejeitado — ver `use_instead` no JSON |
| `equipment-layer.spec.template.json` | Template futuro (fase 2) — não usar no alpha |
